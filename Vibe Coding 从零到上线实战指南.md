# Vibe Coding 从零到上线实战指南

> 本文整理自卡兹克（AIHOT 作者）的实战经验。AIHOT 从 2025 年 5 月发布，2 个多月做到周活 30 万、月活 60 万。作者为纯外行背景，以下流程面向完全不懂代码、想先把第一个产品做出来的人。
>
> **核心理念**：你可以不会写代码，但必须知道逻辑怎么走。AI 是执行者，你才是知道怎么做的人。

---

## 准备工作

### Step 1：购买国产大模型的 Coding Plan 套餐

你需要一个能帮你写代码的 AI。

- **可选**：Kimi、GLM、Qwen 等国产大模型的 Coding Plan
- **原则**：能买到哪个就用哪个，差距没有你想象的大

---

### Step 2：下载官方 Agent 编程产品

买了 Coding Plan 之后，下载对应厂商的官方 Agent 编程工具。

- **对应关系**：
  - Kimi → Kimi Code
  - Qwen → Qoder
  - GLM → ZCode
- **原则**：未来肯定是自家的 Agent 更适合自家的大模型，自家的 Harness 也会快速补上

---

### Step 3：想好你的产品名字

**这一步极其重要，花 10 分钟能省后面无数麻烦。**

改名成本比你想象的大一百倍——logo、域名、备案、小程序名称、App Store 上架全都跟名字绑定。

**起名 checklist**：
- [ ] 简短、好记、有辨识度
- [ ] 在微信搜索公众号、小程序，确认没有同名
- [ ] 在 App Store 和各大应用商店搜索，确认没有撞车

---

### Step 4：注册域名

名字想好的那一刻，立刻去注册域名。

- **平台**：火山引擎、腾讯云、阿里云均可
- **作者教训**：AIHOT.com 是永远买不起的程度，早期没重视域名

---

### Step 5：购买云服务器

产品做出来之后需要 7x24 小时运行，不能跑在自己电脑上。

- **推荐**：腾讯云轻量服务器，新客一年 99-199 元，轻度产品足够
- **选配置**：截图问你的 AI，告诉它你想做什么产品，让它帮你挑
- **进阶**：让 Agent 操控浏览器直接帮你选

---

### Step 6：同步进行 ICP 备案

**这一步非常重要，必须与开发同步进行。**

在中国大陆，域名对外提供服务必须做 ICP 备案，不备案会被运营商直接拦截。

- **流程**：在买服务器的云平台找到"备案"入口 → 提交身份证、域名信息、网站名称 → 等待审核（一般 1-2 周）
- **出海产品**：不需要备案，但记得买海外服务器（新加坡/美西节点）

---

## 项目启动

### Step 7：新建项目文件夹

在电脑上新建一个文件夹，命名为你的产品名字。

这个文件夹就是你整个产品的家，所有代码、配置、文档都会在这里。

```
AIHOT/          <-- 你的产品名字
├── ...         <-- 所有代码和文档
```

---

### Step 8：启动 Agent，开始开发

打开 Step 2 装好的 Agent 编程产品，把工作目录指向你刚才建的文件夹。

从这一刻开始，AI 就驻扎在你的项目里了，它能：
- 看到你的文件
- 帮你创建新文件
- 帮你写代码、跑代码、调代码

---

### Step 9：注册 GitHub 并初始化仓库

**代码不能只存在自己电脑上**，硬盘坏了、电脑丢了就什么都没了。

1. 注册一个免费的 GitHub 账号
2. 让 Agent 帮你连接 GitHub 账号
3. 创建一个**私有仓库**（注意是私有，不是公开）

**给 Agent 的指令**：

```
初始化 Git 仓库，连接我的 GitHub 账号，创建一个私有 GitHub 仓库，并提交第一个初始版本。
```

现在，你和 Agent 的协作正式开始了。

---

## 核心开发流程

### Step 10：开启 Plan 模式，说清楚你要做什么

大多数 Agent 产品都有一个 **Plan 模式（规划模式）**。

**不要直接开始执行**，先开启 Plan 模式，用最简单直白的语言描述你的产品。

**原则**：
- 不要想太多
- 不需要画原型图
- 不需要写需求文档
- 不需要列所有功能
- **只说最核心的那个东西**，后面的功能慢慢加

> **先看到产品出来的正反馈，比什么都要强。**

AI 会根据你的描述生成一份规划文档，确认方向没问题后进入下一步。

---

### Step 11：让 Agent 根据 Plan 文档执行开发

确认 Plan 之后，告诉 Agent 开始执行。

它会自动：
- 创建项目结构
- 写代码
- 安装依赖

你什么都不用管，看着它干活就行。过程中它做完一块会问你要不要看看效果，或者有没有什么要调整的。

---

### Step 12：本地验收

Agent 开发到一定程度后，会给你一个本地预览地址（类似 `localhost:3001`）。

在浏览器打开这个地址，就能看到你的产品长什么样。

**验收原则**：
- UI 不好看 → 让 AI 改
- 功能逻辑不对 → 让 AI 修
- **你是产品经理，AI 是执行者，你觉得对就是对**

---

### Step 13：跑洁癖 Skill，规范化整理

这是作者用得最多的 Vibe Coding 专用 Skill。

开发多轮之后，代码和文档大概率已经有点乱了，甚至出现自相矛盾的情况。洁癖 Skill 专门做一件事：**把项目文档、代码规则、AI 记忆文件全部规范化整理一遍**。

**为什么必须做**：
- AI 的记忆是有限的
- 新开一轮对话，前面聊的很多东西它就忘了
- 如果没有干净、无矛盾的项目文档兜底，AI 会开始瞎编
- 它会忘了你之前的设计决策、产品逻辑，然后写出跟之前完全矛盾的代码

**开源地址**：https://github.com/KKKKhazix/khazix-skills/tree/main/neat-freak

**操作流程**：
1. 跑完洁癖 Skill
2. 结束当前对话
3. 以后每次开新对话，AI 只需要读一遍项目里的文档，就能快速恢复上下文

---

## 规范化与测试

### Step 14：学一些专业名词

你不需要懂代码，但需要懂一些名词，因为后面的流程会经常用到。

| 名词 | 简单理解 |
|------|----------|
| Git | 代码版本管理工具 |
| 分支（Branch） | 代码的平行线，改坏了不影响主线 |
| 主分支（main） | 最稳定、能跑的那条线 |
| PR（Pull Request） | 申请把分支的改动合并到主分支 |
| CI（Continuous Integration） | 自动跑测试，确认代码没问题 |

**学习方式**：直接让 Agent 给你解释，不需要完全搞懂，但要知道这些词在说什么。

---

### Step 15：建立测试流程（强烈建议开 GitHub Pro）

Agent 开发非常快，所以测试很重要。

**给 Agent 的指令**：

```
帮我根据我的项目搭一套完整的测试流程。
```

**GitHub Pro 会员（4 美元/月）的核心价值——分支保护**：

开启分支保护后：
- 任何人（包括你自己）**无法直接往主分支推代码**
- 所有改动必须走 PR
- PR 必须通过 CI 测试才能合并

**没有分支保护的风险**：
- 你或 Agent 手一抖，把有问题的代码推到主分支
- 线上直接崩掉
- 或者自己跟自己对抗，搞一套复杂的维护脚本

**给 Agent 的指令（买了 Pro 后）**：

```
我买了 GitHub Pro 会员，帮我开启分支保护，再给我的项目做好全面且规范、专业的测试流程。
```

---

## 部署上线

### Step 16：部署到服务器

新起一个对话，把服务器 IP 地址发给 Agent。

**给 Agent 的指令**：

```
我要把这个项目部署到这台服务器上：[你的服务器IP]
```

Agent 会处理好一切。有问题的话，登录云厂商控制台，让它自己操控浏览器去解决。

部署完成后，你的代码就会在服务器上 7x24 小时自动运行了。

---

### Step 17：绑定域名并配置 HTTPS

告诉 Agent 你的域名，让它帮你：
- 把域名指向服务器
- 搞一个免费的 SSL 证书
- 配置好 HTTPS

**给 Agent 的指令**：

```
我的域名是 [你的域名]，帮我把它指向这台服务器，配置好 HTTPS。
```

做完之后，在浏览器输入你的域名，就能看到你的产品了。

> **第一次在浏览器里输入自己的域名，看到自己搓出来的产品真的能打开，那个感觉绝对超级爽。**

---

## 持续迭代

### Step 18：进入固定迭代循环

上线不是终点，是起点。日常工作流固定为：

```
新建分支 → 说出你的需求 → Agent 开发完成 → 跑一遍洁癖 Skill 统一文档 → 提交 PR 过 CI 测试 → 合并到主分支 → 部署上线
```

**这个流程的好处**：
- 看着复杂，跑两三遍就肌肉记忆了
- **安全**：每次改动都在分支上，不会直接动主分支
- 改坏了就扔掉分支重新来，主分支永远是稳定能跑的版本
- 可以稳定地并发同时推进三四个任务

---

### Step 19：必须懂架构，可以不懂代码

**这是整篇文章最重要的一段话。**

你可以不会写代码，可以让 AI 帮你写所有代码，但你**必须知道你的产品到底是怎么运转的**：

- 每个前端页面背后对应哪个功能
- 每个后端逻辑做了什么事情
- 数据库有哪些表
- 每张表里存了哪些字段
- 这些字段是怎么被用到的
- 用户点了一个按钮之后，数据是怎么流转的

**为什么必须清楚**：
- 如果你不清楚，就丧失了对产品的判断力
- AI 说"我改好了"，你连验收都验收不了
- AI 给你一个技术方案，你分不清它是合理建议还是在用复杂的东西糊弄你
- **不要让渡自己的思考**

**作者的做法**：
每次跟 AI 讨论架构方案，都会让它把方案解释清楚——为什么用这个方案不用那个方案，好处是什么坏处是什么。听明白了、同意了，或者认为有更好的方案，才会让它动手。

> **它要求你在完全不懂技术的前提下，去理解一个技术系统的运作方式。但这恰恰是 Vibe Coding 最核心的能力。**

---

### Step 20：学习运维知识

产品刚上线可能就几十个人用，服务器随便扛。但用户多起来之后：

- 缓存机制
- 防爬虫
- 防 DDoS
- 流量带宽的费用控制

这些早期完全不用管，但用户量过了一定级别之后，不管就来干你。

**作者教训**：不要被人一天刷了 4000 块的账单才想起来学，防范意识要趁早。

---

## 核心原则总结

| 原则 | 说明 |
|------|------|
| **规划先行** | 用 Plan 模式先想清楚，再让 AI 执行 |
| **正反馈驱动** | 第一版可以简单，先看到产品跑起来 |
| **文档兜底** | 每次开新对话前跑洁癖 Skill，确保 AI 上下文干净 |
| **分支保护** | 主分支永远是稳定的，所有改动走 PR + CI |
| **架构必须懂** | 可以不会写代码，但必须知道数据怎么流转 |
| **不让渡思考** | AI 是执行者，你才是知道怎么做的人 |

---

## 快速启动 Checklist

- [ ] 购买国产大模型 Coding Plan
- [ ] 下载对应 Agent 编程产品
- [ ] 确定产品名字并查重
- [ ] 注册域名
- [ ] 购买云服务器
- [ ] 提交 ICP 备案（国内产品）
- [ ] 新建项目文件夹
- [ ] 启动 Agent 并指向项目文件夹
- [ ] 注册 GitHub 并创建私有仓库
- [ ] 开启 Plan 模式描述产品
- [ ] 让 Agent 执行开发
- [ ] 本地验收调试
- [ ] 跑洁癖 Skill 整理文档
- [ ] 建立测试流程
- [ ] 部署到服务器
- [ ] 绑定域名 + HTTPS
- [ ] 进入迭代循环

---

> **写在最后**
>
> 两年前，你要搓一个能上线的产品，得学编程、学框架、学运维、学数据库，光准备和学习就能劝退 99.99% 的人。
>
> 现在你需要的只有三样东西：**一个想法，一个 AI 编程账号，一台几十块钱的服务器**。
>
> 然后，就开始做。
>
> AI 会帮你解决一切。
>
> 这就是最好的黄金时代。
