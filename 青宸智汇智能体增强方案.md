# 青宸智汇 AI 智能体增强方案（参考 EvoFlow）

> 参考基准：`EvoFlow智能体拆解与设计文档.md`（EvoFlow / EvoPanel v0.3.8，Tauri v2 + LangGraph 超级智能体编排框架）
> 目标项目：`ai-mate/react-ai-chat/react-ai-chat`（青宸智汇 Chat，React 19 + antd 6 + Zustand + Vite）
> 文档性质：差距分析与可落地增强建议，所有建议标注优先级（P0 立即 / P1 近期 / P2 中期 / P3 远期）与落地位置

---

## 一、青宸智汇现状与 EvoFlow 的能力差距

青宸智汇已具备 4 个 AI 角色（探路者/军师/工匠/管家）、Skill 库、MCP 配置、自动化规则、模型管理五大模块的界面骨架，但引擎层大多未接通：真实后端仅 `/api/ai/chat` 一个端点（转发火山方舟），流式接口已封装但无人使用，MCP 连接、自动化执行、任务看板均为本地 mock，对话与业务数据全部驻留内存。

EvoFlow 与之同属"Agent 工具箱 + 多角色 + 技能/工具/自动化"的产品形态，但它是可运行的完整引擎。逐项对照后，差距集中在六个方向，下文按优先级展开：

| EvoFlow 能力 | 青宸智汇现状 | 差距级别 |
| --- | --- | --- |
| SSE 流式输出 | 已封装 `chatWithZhipuStream`，全项目零调用 | 轻微（接通即可） |
| 工具调用（function calling） | 请求体无 tools 字段，MCP 全 mock | 核心 |
| 对话持久化 | conversations 仅存 Zustand 内存，刷新即丢 | 核心 |
| 计划模式 / 任务 DAG | 无；自动化 `maxIterations` 仅展示 | 核心 |
| 记忆系统 | 无 | 重要 |
| 知识库 / RAG | 无 | 重要 |
| 技能渐进式加载 | Skill 仅作 prompt 模板前端替换 | 中等 |
| 澄清机制 | 无 | 中等 |
| 自动化真实调度 | schedule 触发器无调度器，动作无实现 | 中等 |
| 应用中心（工作流固化） | 无 | 远期 |
| IM 渠道 / 智能体员工 / 语音 | 无 | 远期 |

---

## 二、P0：立即落地，投入产出比最高

### 2.1 接通流式输出

EvoFlow 前端用 EventSource 消费 SSE，事件类型为 `message / tool_call / tool_result / error / done`，断线指数退避重连（1s×2^n，上限 30s）。青宸智汇的 `chatWithZhipuStream` 已实现 SSE 解析（`aiService.ts:152`），后端 server.js 支持 `stream` 参数透传，缺口只在 UI 层。

落地位置：`components/ChatLayout.tsx`（对话消息区）与 `components/AIGeneratorForm.tsx`（生成表单）改调流式接口，在消息气泡内追加 `TypingIndicator`（组件已存在）。预期收益：对话体感从"等待整包返回"变为"边生成边渲染"，配合现有 `page-fade-in` 动画即可显著提升完成度。

### 2.2 打通对话持久化

EvoFlow 以 LangGraph thread 为核心，每条消息按 message id 去重追加，线程状态含 messages、artifacts、todos、uploaded_files 等字段。青宸智汇最直接的第一步：把 `aiStore` 的 conversations 落到 `localStorage`（键名沿用现有 `ai-mate-settings` 风格，如 `ai_mate_conversations_v1`），会话列表按时间倒序，支持重命名与删除。

中期的第二步是在后端新增对话表（现有 `db.js` 已接 MySQL，有 `database/schema.sql`），以 `thread_id` 维度存储消息，为后续记忆与任务关联打基础。持久化是记忆、计划、任务中心一切能力的前提，应最先做。

### 2.3 工具调用（function calling）

这是青宸智汇从"聊天机器人"跨向"智能体"的分水岭。EvoFlow 的工具系统有清晰的加载次序：内置工具（ask_clarification / view_image / task）→ 配置工具组（read_file / write_file / bash / ls）→ MCP 工具 → 子代理工具，统一以 OpenAI function calling 格式暴露给模型，工具执行状态三态（success / error / interrupted）。

落地路径（不引入重型后端即可起步）：

1. `server.js` 的 `/api/ai/chat` 在请求体透传 `tools` 数组，响应中若 `finish_reason === 'tool_calls'`，把 `tool_calls` 原样返回给前端。
2. 前端新增 `ToolExecutor` 轻量模块，维护"工具名 → 处理函数"注册表。首批实现三个对创业场景最有价值的工具：
   - `web_search`：调用现成的 Web 搜索接口，返回摘要列表（对应探路者AI 的行业/竞品调研）。
   - `fetch_url`：抓取指定 URL 正文摘要（EvoFlow 用 `https://r.jina.ai/` 实现，可复用该思路）。
   - `current_time` / `calc` 等确定性小工具，用于验证链路。
3. `ChatLayout` 的请求循环升级为：发送 messages+tools → 收到 tool_calls → 执行工具 → 追加 tool 结果消息 → 再次请求模型，最多 N 轮（`toolCallRounds` 已存在于 `ModelConfig` 类型，默认建议 3）。
4. MCP 面板的 `ToolTestPanel` 从 mock 改为调用 `ToolExecutor`，先把"工具测试"做成真链路，再逐步接 MCP 客户端进程。

预期收益：探路者AI 的竞品调研、军师AI 的策略分析可从"表单驱动"升级为"模型自主调研"，四个角色共享同一工具栈，与 EvoFlow 的"工具渐进暴露"思路一致。

---

## 三、P1：近期落地，把任务能力做深

### 3.1 计划模式（Plan）与子任务 DAG

EvoFlow 的 Plan 模式是核心差异化：Supervisor 澄清意图 → 调用 `plan()` 生成可审阅计划（带 boundTaskId）→ 用户授权 → 按 `depends_on` 依赖顺序派发子任务 → 完成闭环。它有三道保护：PlanGuardMiddleware 在授权前过滤高副作用工具，确认条（查看计划 / 开始执行 / 另存为应用），以及步骤执行人可指定角色。

青宸智汇的适配方案：

1. 在 `NewConversation` 或 `ChatLayout` 增加"计划模式"开关，模型请求追加系统提示（参考 EvoFlow 的 CLARIFY→PLAN→ACT 工作流），要求先输出结构化 JSON 计划：`{ goal, steps: [{ id, title, depends_on[], assigned_role, acceptance }] }`。
2. 前端渲染计划卡片（antd `Steps` 或 `Timeline`），用户点"开始执行"后才逐步骤调用对应角色执行。
3. 执行结果回填到任务列表，步骤间按 `depends_on` 自动推进。
4. 任务看板（`components/butler/TaskBoard.tsx`）从本地 state 升级为消费同一任务数据源，四个角色的执行记录统一归档。

此功能与青宸智汇四个角色的分工天然契合：计划可自动把"市场调研→策略规划→内容制作→项目落地"分派给探路者/军师/工匠/管家，形成多角色协作流水线，正是 EvoFlow"Agent Teams"的本土化形态。

### 3.2 自动化真实执行

现状：`automationStore.ts:155` 的 `executeRule` 为 800ms 延迟 + 随机成功，schedule 触发器无调度器，新建规则时 `actions` 恒为空数组。EvoFlow 的 `AutomationScheduler` 跑在 asyncio 事件循环，按 tick（默认 30s）扫描 cron 任务，支持 fresh / sticky 两种线程模式，结果可推送渠道。

落地路径：

1. 后端 `server.js` 增加定时任务模块：一个轻量 cron 解释器（五字段，前端 `schedule` 触发器已定义该结构），每 30s tick 扫描到期任务。
2. 动作执行打通真实能力：`send-message` → 调 `/api/ai/chat` 生成内容；`invoke-skill` → 调用 skillStore 的 prompt 模板；`invoke-mcp` → 调 `ToolExecutor`。`switch-role` / `set-variable` 保持前端本地动作。
3. 自动化规则的 CRUD 迁移到后端持久化（`automationStore` 增加 localStorage 或后端同步），执行日志写入时间轴真实记录。
4. 修复"actions 恒为空"的编辑器缺口：`Automation.tsx:111` 处补全动作配置表单（类型、参数、目标角色）。

### 3.3 会话级上下文管理

EvoFlow 有 SummarizationMiddleware：接近 Token 上限时自动压缩，最近 10 轮完整保留、更早转摘要。青宸智汇当前全量历史拼装，长对话必然超限。建议在 `chatWithZhipu` 封装层增加上下文裁剪策略：保留最近 N 轮完整消息，更早消息用 `system_prompt` 追加"以上是更早对话摘要：..."占位，或直接丢弃。这是成本与体验的双赢项，实现成本低。

---

## 四、P2：中期落地，形成差异化

### 4.1 记忆系统

EvoFlow 记忆系统设计值得完整借鉴：MemoryMiddleware 在对话完成后过滤"用户输入 + 最终 AI 回复"入队 → 防抖 30s → LLM 提取结构化事实（category 分为 preference / knowledge / context / behavior / goal）→ 原子写入 memory.json → 下次对话注入 `<memory>` 标签（最多 15 条事实，Token 上限 2000）。

青宸智汇的轻量实现：

1. 后端新增 `/api/memory` 端点：对话结束时异步调 LLM 提取事实，存 JSON（沿用 EvoFlow 的 facts 结构：`{ id, content, category, confidence, source }`）。
2. 每次请求模型前，把该用户最近 15 条高置信度事实拼入 system_prompt。
3. 前端设置抽屉增加"记忆管理"入口（查看 / 删除单条事实），对应 EvoFlow 的 `/api/memory/facts/{id}`。

对创业场景的实用价值：军师AI 记住用户的创业方向与偏好后，后续策略建议无需重复交代背景。

### 4.2 知识库 / RAG

EvoFlow 知识库连接本机 Obsidian / Markdown 目录，检索模式全文 / 语义 / 混合 / 标题。青宸智汇面向大学生创业，最直接的落地形态是"平台内置创业资料库"：预置 BP 模板、行业报告、政策文件等文档，建立向量索引（可用 sqlite-vec 或轻量方案，不必上 pgvector）。

实现路径：`server.js` 增加 `/api/knowledge/search` 端点（关键词检索起步，语义检索后置），前端在 SkillLibrary 旁新增"知识库"页；探路者AI 的行业报告、军师AI 的策略输出可将检索结果作为上下文注入。优先级可放在记忆系统之后，两者共享同一上下文注入链路。

### 4.3 技能渐进式加载与自动触发

EvoFlow 技能机制的精髓是"渐进式暴露"：系统提示只注入技能名称与描述（`<available_skills>`），模型按需 `read_file` 读取 SKILL.md，避免一次性注入全部技能浪费 Token。青宸智汇现有 30+ Skill 模板，但触发完全依赖 `/cmd` 手动输入，`autoTriggers` 字段（keyword / intent / regex）未被使用。

落地建议：在模型请求前，用轻量规则匹配用户输入与 `autoTriggers`（关键词精确匹配即可起步，intent 匹配后置），命中则自动注入对应 prompt 模板。这样技能从"手动命令"进化为"情境自动触发"，与 EvoFlow 的意图驱动一致，且不改动现有架构。

### 4.4 澄清机制

EvoFlow 将澄清列为强制场景：信息缺失 / 需求含糊 / 方案选择 / 风险确认 / 建议确认，走 CLARIFY→PLAN→ACT。青宸智汇的 `ask_clarification` 可以在后端未完成时先做前端版：当模型输出中包含"需要澄清"标记（约定 JSON 格式），前端渲染提问卡片而不是直接提交，用户回答后携带上下文续跑。这对生成类表单（BP 撰写、项目计划）能显著减少无效生成。

---

## 五、P3：远期储备，按产品方向选择性落地

| 能力 | EvoFlow 实现 | 青宸智汇落地条件 | 参考价值 |
| --- | --- | --- | --- |
| 应用中心 | 将跑通的工作流固化为可复用应用，填参再跑 | 先有真实的 Plan/任务执行后自然沉淀 | 高，是"从对话到产品"的跃迁 |
| 多角色协作 DAG | Agent Teams + 步骤指派角色 + 依赖推进 | 依赖 3.1 计划模式成熟 | 高，契合四角色产品形态 |
| 智能体员工 | 岗位合同（职责/频率/审批），到点主动干活 | 依赖自动化真实执行 + 任务持久化 | 中，适合团队版规划 |
| 定时报告推送 | 自动化 + IM 推送 | 依赖渠道接入 | 中 |
| IM 渠道 | 飞书/钉钉/Telegram 出站连接 | 需确定目标渠道（大学生场景可能微信小程序更合适） | 视场景 |
| 语音交互 | 唤醒词 / push-to-talk | 纯前端即可起步（Web Speech API），但优先级低 | 低 |
| 模型可观测 | Agent Trace，逐请求 Token 用量 | `ZhipuResponse.usage` 已返回，前端展示成本极低 | 高，可提前做 |

其中"模型可观测"建议提前：后端已返回 `usage`（prompt/completion/total tokens），前端只需在消息详情或设置页加一个用量统计展示，即可让用户感知成本与上下文消耗，同时为后续记忆注入的 Token 预算管理打底。

---

## 六、建议落地顺序与里程碑

| 阶段 | 内容 | 依赖 | 验收标志 |
| --- | --- | --- | --- |
| 阶段一（P0） | 流式接通、对话持久化、工具调用链路 | 无 | 对话流式渲染；刷新页面会话保留；探路者AI 可自主完成一次真实网页调研 |
| 阶段二（P1） | 计划模式 + 子任务推进、自动化真实执行、上下文裁剪 | 阶段一 | 用户授权后四角色按 DAG 自动协作；定时任务到点真实执行 |
| 阶段三（P2） | 记忆系统、知识库 RAG、技能自动触发、澄清机制 | 阶段二（记忆依赖持久化） | 军师AI 跨会话记住创业偏好；行业报告检索可注入上下文 |
| 阶段四（P3） | 应用中心、模型用量展示、渠道推送 | 阶段二/三 | 一次成功的 Plan 可另存为应用并再次运行 |

两个原则贯穿全程：一是保持"界面层先行、引擎层逐步替换 mock"的既有节奏，所有新增能力都从现有页面骨架切入；二是先做对用户可感知的体验（流式、持久化），再做引擎深度（计划、记忆），避免一次性引入 EvoFlow 式重型后端造成维护断层。

---

## 七、风险与约束

| 风险 | 说明 | 缓解 |
| --- | --- | --- |
| 火山方舟 plan/v3 是否支持 function calling 与 tools 透传 | 后端 `callArkAPI` 目前仅转发 chat/completions | 先在 ToolTestPanel 做一次最小验证；若不支持，可先走"模型输出 JSON → 前端解析执行"的伪工具方案 |
| 流式 + 工具调用的协议衔接 | SSE 事件需扩展 tool_call 类型 | 沿用 EvoFlow 事件四元组（message/tool_call/tool_result/error）规范前端解析层 |
| 任务与对话数据上云 | 目前全本地存储，多端同步需求未定 | 阶段一用 localStorage 过渡，schema.sql 已预留后端表结构 |
| 多角色共享工具的执行边界 | 工具误用可能产生副作用 | 参照 EvoFlow，计划未授权前限制高副作用工具，工具执行前二次确认 |
