# 3D层叠项目文档卡片堆 - 实现修改文档

## 一、功能概述

在工匠AI的「产品文档」面板中实现3D层叠项目文档卡片堆组件，支持8-10张卡片呈3D斜向扇形层叠堆叠，具备拖拽拉出、点击查看、自动归位、悬停反馈等完整交互逻辑。

## 二、文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/components/maker/doc-card-storage.ts` | 新增 | 卡片数据持久化（localStorage） |
| `src/components/maker/doc-card-stack.css` | 新增 | 3D布局、磨砂玻璃样式、动画 |
| `src/components/maker/DocCardStack.tsx` | 新增 | 3D卡片堆核心组件 |
| `src/components/maker/ProductDoc.tsx` | 修改 | 集成3D卡片堆到产品文档面板 |
| `src/components/AIGeneratorForm.tsx` | 修改 | 新增 `extraContent` prop |

## 三、详细修改

### 3.1 `doc-card-storage.ts`（新增）

**职责**：卡片数据的 CRUD 持久化，遵循项目现有 `proto-storage.ts` / `demo-storage.ts` 模式。

**核心接口**：
```typescript
export interface DocCardData {
  id: string;                  // 唯一标识
  title: string;               // 文档标题
  progress: number;            // 完成进度百分比 (0-100)
  status: 'draft' | 'review' | 'completed';  // 状态标签
  content: string;             // 文档Markdown内容（供详情面板展示）
  sections: string[];          // 章节标题列表（供缩略纹理展示）
  createdAt: number;           // 创建时间戳
  updatedAt: number;           // 更新时间戳
}
```

**持久化规则**：
- localStorage key: `ai-mate-maker-doc-cards`
- 最大卡片数: 10张（FIFO淘汰）
- 损坏数据自动回退为空集合

### 3.2 `doc-card-stack.css`（新增）

**职责**：实现暗黑科技B端风格的3D层叠卡片视觉效果。

**关键样式**：

```css
/* 透视舞台 - 1200px透视距离 */
.doc-card-stage {
  perspective: 1200px;
  perspective-origin: 50% 80%;
  height: 460px;
  contain: layout style paint;
}

/* 3D空间层 - 保持子元素3D变换 */
.doc-card-layer {
  transform-style: preserve-3d;
}

/* 单张卡片 - 磨砂玻璃拟态 */
.doc-card {
  position: absolute;
  inset: 0;
  border-radius: 16px;
  transform-style: preserve-3d;
  backface-visibility: hidden;
  transform-origin: bottom center;
  will-change: transform, opacity;

  /* 玻璃态背景：半透明深色渐变 */
  background: linear-gradient(135deg, rgba(30,30,45,0.65), rgba(20,20,35,0.75));
  backdrop-filter: blur(16px) saturate(180%);

  /* 微光边框 + 柔和投影 */
  border: 1px solid rgba(255,255,255,0.08);
  box-shadow: 0 8px 32px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.05);

  /* GPU加速过渡 - 仅动画 transform/opacity/filter */
  transition: transform 300ms cubic-bezier(0.4,0,0.2,1),
              filter 200ms ease,
              box-shadow 300ms cubic-bezier(0.4,0,0.2,1);
}

/* 激活/拖拽态：荧光绿发光 */
.doc-card.active, .doc-card.dragging {
  border-color: rgba(0,255,136,0.5);
  box-shadow: 0 16px 48px rgba(0,0,0,0.6),
              0 0 24px rgba(0,255,136,0.4),
              0 0 48px rgba(0,255,136,0.2);
  filter: brightness(1.2);
}

/* 悬停态：前浮提亮 */
.doc-card.hovered {
  filter: brightness(1.15);
  border-color: rgba(8,145,178,0.3);
}

/* 拖拽中：禁用过渡，非激活卡片降低亮度 */
.doc-card.dragging { transition: none !important; cursor: grabbing; }
.doc-card-layer.is-dragging .doc-card:not(.dragging) {
  pointer-events: none;
  filter: brightness(0.6);
}
```

**亮色模式适配**：通过 `.light-mode` 类切换为白色系玻璃态。

**响应式**：移动端降低模糊强度（`blur(10px)`），缩小卡片尺寸。

**动效降级**：`prefers-reduced-motion` 下过渡时长降至0.01ms。

### 3.3 `DocCardStack.tsx`（新增）

**职责**：3D卡片堆核心组件，实现全部交互逻辑。

#### 3.3.1 扇形布局算法

```typescript
/**
 * 计算卡片在扇形布局中的 3D transform
 * 以底部为轴心，左右对称展开，Z轴层叠形成纵深
 */
function getCardTransform(index: number, total: number): string {
  if (total <= 1) return 'rotateX(-4deg)';
  const center = (total - 1) / 2;
  const offset = index - center;
  const angleY = (offset / center) * 12;        // ±12°扇形展开
  const translateZ = -Math.abs(offset) * 8;      // Z轴层叠（越靠边越深）
  const angleX = -4 + Math.abs(offset) * 0.5;    // 前倾
  return `rotateY(${angleY}deg) translateZ(${translateZ}px) rotateX(${angleX}deg)`;
}
```

#### 3.3.2 核心拖拽逻辑

**设计思路**：使用 `useRef` 存储拖拽过程数据 + `requestAnimationFrame` 节流 + 直接操作 DOM style，实现零React重渲染的60fps拖拽。

```typescript
// 拖拽过程数据（存储在 useRef 中，不触发重渲染）
interface DragState {
  startIndex: number | null;
  startX: number;
  startY: number;
  offsetX: number;
  offsetY: number;
  hasMoved: boolean;
}

// 1. 鼠标按下：记录起点，绑定 document 事件
const handleMouseDown = (e: React.MouseEvent, index: number) => {
  e.preventDefault(); // 阻止默认选中和拖拽行为
  dragState.current = { startIndex: index, startX: e.clientX, startY: e.clientY, ... };
  document.addEventListener('mousemove', handleMouseMove);
  document.addEventListener('mouseup', mouseUpRef.current);
};

// 2. 鼠标移动：rAF节流，直接操作DOM style
const handleMouseMove = (e: MouseEvent) => {
  ds.offsetX = e.clientX - ds.startX;
  ds.offsetY = e.clientY - ds.startY;

  // 阈值检测：首次越过5px时切换为拖拽态
  if (!ds.hasMoved && distance > DRAG_THRESHOLD) {
    ds.hasMoved = true;
    setIsDragging(true);
  }

  // rAF节流：直接操作 DOM style，零重渲染
  requestAnimationFrame(() => {
    cardEl.style.transition = 'none';
    cardEl.style.transform = buildDragTransform(baseTransform, offsetX, offsetY);
    cardEl.style.zIndex = '1000';
  });
};
```

#### 3.3.3 核心归位逻辑

**设计思路**：拖拽松开时清除内联样式，CSS transition（300ms ease-out）自动驱动卡片回到原始位置。

```typescript
// 3. 鼠标松开：清除内联样式 → CSS transition 自动回弹
const handleMouseUp = () => {
  document.removeEventListener('mousemove', handleMouseMove);
  document.removeEventListener('mouseup', mouseUpRef.current);

  if (ds.hasMoved) {
    // 拖拽结束：清除内联样式，CSS transition 自动回弹
    const cardEl = cardRefs.current[ds.startIndex];
    cardEl.style.transition = '';   // 恢复CSS定义的300ms ease-out
    cardEl.style.transform = '';    // 清除内联transform，回到CSS class的原始位置
    cardEl.style.zIndex = '';       // 恢复原始层级
  }
  // 重置状态...
};
```

#### 3.3.4 点击与拖拽区分

**设计思路**：使用 `wasDragRef` 记录最后一次交互是否为拖拽，`onClick` 事件在 `mouseup` 之后触发，检查 ref 判断是点击还是拖拽。

```typescript
// mouseup 中记录是否为拖拽
wasDragRef.current = ds.hasMoved;

// onClick 处理器（click 在 mouseup 之后触发）
onClick={() => {
  if (!wasDragRef.current) {
    setSelectedCard(card);  // 非拖拽 → 打开详情面板
  }
  wasDragRef.current = false;
}}
```

#### 3.3.5 `mouseUpRef` 模式

**问题**：`useCallback` 的 `handleMouseUp` 需要在 `handleMouseDown` 中引用，但 `handleMouseUp` 又依赖 `cards` 和 `handleMouseMove`，形成循环引用。

**解决**：使用 `useRef` 存储 `handleMouseUp` 的最新引用，在 `useEffect` 中同步更新。

```typescript
const mouseUpRef = useRef<(e: MouseEvent) => void>(() => {});

const handleMouseUp = useCallback(() => { /* ... */ }, [cards, handleMouseMove]);

// 在 effect 中同步 ref（避免渲染期间访问 ref）
useEffect(() => {
  mouseUpRef.current = handleMouseUp;
}, [handleMouseUp]);

// handleMouseDown 中使用 ref
document.addEventListener('mouseup', mouseUpRef.current);
```

### 3.4 `AIGeneratorForm.tsx`（修改）

**修改内容**：新增 `extraContent` prop，用于渲染独立于文档生成结果的额外内容。

```typescript
interface AIGeneratorFormProps {
  // ...现有 props...
  /** 额外内容：渲染在结果区域之后，独立于文档生成结果（如3D卡片堆） */
  extraContent?: React.ReactNode;
}
```

在 maker 变体的渲染中，`extraContent` 放在加载态之后：

```tsx
{/* 额外内容（独立于生成结果，如3D卡片堆） */}
{extraContent && (
  <div style={{ marginTop: 16 }}>
    {extraContent}
  </div>
)}
```

### 3.5 `ProductDoc.tsx`（修改）

**修改内容**：
1. 从箭头函数直接返回改为带状态的函数组件
2. 添加3D卡片堆状态管理和数据持久化
3. 保留原有文档结构树和导出功能
4. 在 `resultRenderer` 中添加"添加到3D文档卡片堆"按钮
5. 通过 `extraContent` 渲染3D卡片堆（独立于文档生成结果）

```typescript
const ProductDoc: React.FC = () => {
  const isDark = useAIStore((s) => s.settings.theme === 'dark');
  const mTheme = MAKER_THEMES.doc;
  const [cards, setCards] = useState<DocCardData[]>(() => loadDocCards().cards);

  // 添加文档到卡片堆
  const handleAddToStack = useCallback((result: string) => {
    const sections = splitByH2(result);
    const newCard: DocCardData = {
      id: `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`,
      title: /* 从Markdown提取 */,
      progress: Math.min(sections.length * 11, 100),
      status: 'draft',
      content: result,
      sections: sections.map(s => s.title.replace(/^#{1,6}\s*/, '').trim()),
      createdAt: Date.now(),
      updatedAt: Date.now(),
    };
    addDocCard(newCard);
    setCards(loadDocCards().cards);
  }, []);

  return (
    <AIGeneratorForm
      // ...保留原有表单和systemPrompt...
      resultRenderer={(result, { theme }) => (
        <>
          {/* 保留：文档结构树 */}
          <MakerSection title="文档结构" subtitle="FOLDER TREE" ...>
            <DocTree result={result} ... />
          </MakerSection>

          {/* 新增：添加到3D卡片堆按钮 */}
          <Button onClick={() => handleAddToStack(result)}>
            添加到3D文档卡片堆
          </Button>
        </>
      )}
      // 新增：3D卡片堆独立渲染（不依赖文档生成结果）
      extraContent={
        cards.length > 0 ? (
          <MakerSection title="文档卡片堆" subtitle="3D CARD STACK" ...>
            <DocCardStack cards={cards} onCardsChange={handleCardsChange} />
          </MakerSection>
        ) : null
      }
    />
  );
};
```

## 四、交互逻辑总结

| 交互 | 触发方式 | 实现方式 | 动画 |
|------|----------|----------|------|
| 悬停反馈 | `onMouseEnter` / `onMouseLeave` | 设置 `hoveredIndex` 状态，CSS `.hovered` 类控制样式 | `filter: brightness(1.15)` 200ms ease |
| 拖拽拉出 | `onMouseDown` → `document.mousemove` | `useRef` 存储偏移量，`rAF` + DOM直操 `transform` | 拖拽中 `transition: none`，实时跟随 |
| 点击查看 | `onClick`（在 `mouseup` 之后） | `wasDragRef` 区分点击/拖拽，非拖拽时打开 Drawer | Drawer 滑入动画 |
| 自动归位 | `document.mouseup` | 清除内联 `style.transform`，CSS `transition` 300ms ease-out 自动回弹 | `transform 300ms cubic-bezier(0.4,0,0.2,1)` |

## 五、性能优化

1. **rAF节流**：拖拽过程中使用 `requestAnimationFrame` 节流 DOM 操作，避免每帧多次重排
2. **DOM直操**：拖拽时直接操作 `element.style`，不触发React重渲染
3. **GPU加速**：仅动画 `transform`、`opacity`、`filter` 等GPU合成属性
4. **CSS contain**：舞台容器使用 `contain: layout style paint` 隔离重排
5. **will-change**：卡片设置 `will-change: transform, opacity` 提前告知浏览器
6. **事件清理**：组件卸载时移除所有 `document` 事件监听器和 `rAF`

## 六、验证结果

| 验证项 | 结果 |
|--------|------|
| TypeScript 编译 (`tsc --noEmit`) | ✅ 通过 |
| ESLint 检查 | ✅ 0 错误 0 警告 |
| 生产构建 (`npm run build`) | ✅ 成功（4.30s） |
| 3D卡片堆渲染（5张卡片） | ✅ 扇形层叠、磨砂玻璃效果 |
| 点击打开详情面板 | ✅ Drawer 显示文档元信息和章节内容 |
| 卡片管理列表 | ✅ 5张卡片含导出/删除按钮 |
| 状态标签显示 | ✅ 草稿/审核中/已完成 三种状态 |
| 进度百分比显示 | ✅ 85%、60%、100%、45%、75% |
| 拖拽提示文字 | ✅ "拖拽卡片移出查看 · 单击卡片打开详情 · 松开自动归位" |
| 文档结构树保留 | ✅ 原有折叠文件夹树功能完整 |
| 添加到卡片堆按钮 | ✅ 生成文档后显示 |

## 七、后续可扩展方向

1. **触摸支持**：添加 `touchstart` / `touchmove` / `touchend` 事件，支持移动端拖拽
2. **卡片排序**：支持拖拽调整卡片在堆叠中的顺序
3. **多选操作**：支持批量导出/删除
4. **搜索过滤**：在管理列表中添加搜索框
5. **动画增强**：卡片入堆时的飞入动画
