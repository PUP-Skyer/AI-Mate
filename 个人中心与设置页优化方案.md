# 个人中心与设置页优化方案

## 一、需求概述

在 react-ai-chat 子应用左侧深色边栏底部新增个人中心入口，支持显示用户头像、昵称、角色标签（免费/Pro）和速通用量。点击后展开 Dropdown 下拉菜单（参考主界面右上角「测试用户」交互模式），包含升级权益、管理账户、消息、语言、主题、设置、退出登录等入口。点击「设置」后从右侧滑出设置 Drawer，内含账号信息、速通用量、隐私模式、消息通知、主题切换、语言设置等完整功能。

---

## 二、当前完成状态

| 层级 | 文件 | 状态 | 说明 |
|---|---|---|---|
| 类型层 | `types/index.ts` | ✅ 已完成 | 已新增 `UserTier`、`UserInfo`、`ThemeType`、`LanguageType`、`AppSettings` |
| 状态层 | `store/aiStore.ts` | ✅ 已完成 | 已扩展 `AIStore`，含 `userInfo`、`settings`、`settingsDrawerOpen`、`unreadMessageCount` 及 actions，支持 localStorage 持久化 |
| 组件层 | `components/UserProfileMenu.tsx` | ⏳ 待创建 | 个人中心触发按钮 + Dropdown 下拉菜单 |
| 组件层 | `components/SettingsDrawer.tsx` | ⏳ 待创建 | 右侧 Drawer，含账号信息、权益、速通、偏好设置、外观设置、退出登录 |
| 集成层 | `App.tsx` | ⏳ 待修改 | 底部插入 `UserProfileMenu`，挂载 `SettingsDrawer` |
| 验证层 | TypeScript 编译 | ⏳ 待执行 | 确认零报错 |

---

## 三、技术栈与约束

- React 18 + TypeScript + Vite
- Ant Design 6（组件库）
- Zustand + Immer（状态管理）
- 侧边栏深色主题（`#141414`），宽度 `200px`，禁止折叠
- 图标库：`@ant-design/icons` v6
- 状态持久化：`localStorage`

---

## 四、组件设计详案

### 4.1 UserProfileMenu.tsx（个人中心下拉菜单）

**定位**：左侧边栏底部常驻组件，点击展开功能菜单。

**触发区 UI**：

```
┌────────────────────────────┐
│  ┌──┐  用户88062778542      │
│  │A │  ⚡ 0         [免费]   │
│  └──┘                      │
└────────────────────────────┘
```

- **Avatar**：黄色圆形背景（`#fadb14`），`size={32}`，fallback 显示用户昵称首字
- **昵称**：单行文本，溢出省略，颜色 `#d9d9d9`，字体 13px
- **速通**：`ThunderboltOutlined` + `quickPassCount`，颜色 `#8c8c8c`，字体 12px
- **标签**：「免费」暗色底（`#2a2a2a`）+ 浅色文字，字体 11px
- **Hover**：背景变为 `#1f1f1f`，圆角 6px，过渡 0.2s

**Dropdown 菜单项**：

| 菜单项 | 图标 | 特殊表现 | 点击行为 |
|---|---|---|---|
| 升级权益 | `ThunderboltOutlined` | 整行按钮样式（`#2a2a2a` 底 + 白色文字） | 预留：打开升级弹窗 |
| 管理账户 | `UserOutlined` | 右侧 `RightOutlined` | 预留：打开账户管理 |
| 消息 | `BellOutlined` | 右侧 `Badge count={unreadMessageCount}` 红点 | 预留：打开消息中心 |
| — | — | `Divider` | — |
| 语言 | `GlobalOutlined` | 右侧显示「简体中文」+ `RightOutlined` | 预留：展开语言子菜单 |
| 主题 | `SunOutlined` / `MoonOutlined` | 右侧显示「亮色」+ `RightOutlined` | 预留：切换主题 |
| 设置 | `SettingOutlined` | 右侧 `RightOutlined` | `toggleSettingsDrawer(true)` |
| 报告问题 | `ExclamationCircleOutlined` | — | 预留：打开反馈弹窗 |
| — | — | `Divider` | — |
| 退出登录 | — | 文字颜色 `#ff4d4f`，居中 | `logout()` |

**技术要点**：
- `Dropdown` 使用 `trigger={['click']}`
- 浮层使用 `getPopupContainer={(trigger) => trigger.parentElement || document.body}` 避免被 Sider 裁剪
- Dropdown 面板背景 `#1f1f1f`，边框 `#2a2a2a`，圆角 8px

---

### 4.2 SettingsDrawer.tsx（设置抽屉面板）

**定位**：右侧滑出的账号与设置面板，宽度 `420px`。

**Drawer 配置**：
- `placement="right"`，`width={420}`
- `title="账号"`，`closable={true}`
- `open` 绑定 `settingsDrawerOpen`，`onClose` 调用 `toggleSettingsDrawer(false)`
- 无 footer，操作按钮内嵌在内容区

**内容区结构**（纵向排列，`padding: 20px 24px`）：

#### ① 用户信息卡片

```
┌─────────────────────────────────────┐
│  ┌────┐  用户88062778542            │
│  │ A  │  [免费]                      │
│  └────┘                              │
└─────────────────────────────────────┘
```

- 左侧 `Avatar`（黄色圆形，`size={48}`）
- 右侧上方昵称（`Text strong`，16px，颜色 `#262626`）
- 右侧下方 `Tag`（「免费」，`#595959` 背景 + `#fff` 文字）

#### ② 账号信息区块（背景 `#fafafa`，圆角 8px，padding 16px）

| 字段 | 显示方式 | 操作 |
|---|---|---|
| 用户 ID | `userInfo.id` 完整显示 | — |
| 手机号 | 脱敏显示（如 `138****8000`） | — |
| 管理账号 | — | `Button`（「管理账号」，`type="default"`，`size="small"`）|
| 当前套餐 | 「免费」文本 | `Button`（「升级权益」，`type="primary"`，`size="small"`，`ghost`）|

#### ③ 速通用量区块

- 标题：「速通用量」（`Text strong`，14px）
- 文案：「体验 TRAE Work，获得速通」（`Text type="secondary"`，12px）
- 用量：`ThunderboltOutlined` + 「速通可用 `userInfo.quickPassCount` 次」
- 右侧：`Button`（「兑换码」，`type="link"`，`size="small"`）

#### ④ 偏好设置区块

- 标题：「偏好设置」（`Text strong`，14px，下方 16px 间距）

| 设置项 | 控件 | 绑定状态 |
|---|---|---|
| 隐私模式 | `Switch`（`size="small"`） | `settings.privacyMode` |
| 消息通知 | `Switch`（`size="small"`） | `settings.notificationsEnabled` |

#### ⑤ 外观设置区块

- 标题：「外观」（`Text strong`，14px）

| 设置项 | 控件 | 绑定状态 |
|---|---|---|
| 主题 | `Segmented`（选项：亮色 / 暗色） | `settings.theme` |
| 语言 | `Select`（选项：简体中文 / English） | `settings.language` |

#### ⑥ 底部操作

- `Button`（「退出登录」，`danger`，`block`，`size="middle"`），点击调用 `logout()`

**状态绑定**：
- 读取：`userInfo`、`settings`、`settingsDrawerOpen`
- 写入：`updateSettings({ theme: 'dark' })`、`toggleSettingsDrawer(false)`

---

## 五、App.tsx 集成方案

### 5.1 引入新组件

```typescript
import UserProfileMenu from './components/UserProfileMenu';
import SettingsDrawer from './components/SettingsDrawer';
```

### 5.2 底部区域改造

将现有底部：

```tsx
{/* 底部 */}
<div style={{ padding: '12px 16px', borderTop: '1px solid #2a2a2a' }}>
  <Text type="secondary" style={{ fontSize: 11, color: '#595959' }}>
    AI Mate v1.0
  </Text>
</div>
```

替换为：

```tsx
{/* 底部：个人中心 + 版本号 */}
<div style={{ padding: '12px 16px', borderTop: '1px solid #2a2a2a', flexShrink: 0 }}>
  <UserProfileMenu />
  <div style={{ marginTop: 8, textAlign: 'center' }}>
    <Text type="secondary" style={{ fontSize: 11, color: '#595959' }}>
      AI Mate v1.0
    </Text>
  </div>
</div>
```

### 5.3 挂载 SettingsDrawer

在 `Layout` 内部、`Sider` 与 `Content` 之间添加：

```tsx
<SettingsDrawer />
```

> Drawer 默认挂载到 `document.body`，位置不影响功能。建议放在 `Sider` 之后、`Content` 之前，保持 JSX 结构清晰。

---

## 六、状态流设计

```
┌─────────────────┐     click      ┌──────────────────┐
│ UserProfileMenu │ ─────────────→ │ toggleSettingsDrawer(true)  │
│   (触发区)       │                │   aiStore.ts                  │
└─────────────────┘                └──────────────────┘
         │                                    │
         │ click「设置」                        │ state.settingsDrawerOpen
         ↓                                    ↓
┌─────────────────┐                ┌──────────────────┐
│  Dropdown 菜单   │                │ SettingsDrawer   │
│                 │                │   (右侧滑出)      │
└─────────────────┘                └──────────────────┘
                                            │
                                            │ onClose / toggleSettingsDrawer(false)
                                            ↓
                                   ┌──────────────────┐
                                   │ 关闭 Drawer       │
                                   └──────────────────┘
```

**数据流**：
1. `UserProfileMenu` 读取 `userInfo`、`unreadMessageCount`
2. 点击「设置」→ 调用 `toggleSettingsDrawer(true)`
3. `SettingsDrawer` 读取 `settingsDrawerOpen`、`userInfo`、`settings`
4. 修改设置 → 调用 `updateSettings(patch)` → 自动持久化到 `localStorage`
5. 点击「退出登录」→ 调用 `logout()` → 清除 `localStorage` → 刷新页面

---

## 七、样式规范

### 7.1 颜色变量

| 用途 | 色值 | 说明 |
|---|---|---|
| 侧边栏背景 | `#141414` | 主背景 |
| 边框/分割线 | `#2a2a2a` | 分隔线、边框 |
| 主文字 | `#d9d9d9` | 菜单文字、昵称 |
| 次要文字 | `#8c8c8c` | 描述、图标 |
| 置灰文字 | `#595959` | 版本号、禁用态 |
| Avatar 背景 | `#fadb14` | 黄色圆形 |
| Hover 背景 | `#1f1f1f` | 悬停状态 |
| 危险文字 | `#ff4d4f` | 退出登录 |

### 7.2 字体规范

| 元素 | 字号 | 字重 |
|---|---|---|
| 昵称 | 13px | 400 |
| 速通/标签 | 11-12px | 400 |
| Drawer 标题 | 16px | 600 |
| 区块标题 | 14px | 600 |
| 正文 | 13-14px | 400 |

### 7.3 间距规范

| 场景 | 数值 |
|---|---|
| 触发区内边距 | `8px 12px` |
| Dropdown 菜单项间距 | `padding: 8px 16px` |
| Drawer 内容区内边距 | `20px 24px` |
| 区块间距 | `margin-bottom: 24px` |
| 表单项间距 | `margin-bottom: 16px` |

---

## 八、实施步骤

### Step 1：创建 UserProfileMenu.tsx
- 路径：`src/components/UserProfileMenu.tsx`
- 内容：触发区 + Dropdown 菜单，绑定 `userInfo`、`unreadMessageCount`、`toggleSettingsDrawer`、`logout`
- 预计代码行数：~120 行

### Step 2：创建 SettingsDrawer.tsx
- 路径：`src/components/SettingsDrawer.tsx`
- 内容：Drawer 容器 + 用户信息卡片 + 账号信息 + 速通用量 + 偏好设置 + 外观设置 + 退出登录
- 预计代码行数：~200 行

### Step 3：修改 App.tsx
- 引入 `UserProfileMenu` 和 `SettingsDrawer`
- 替换底部版本号为「个人中心 + 版本号」
- 挂载 `SettingsDrawer`
- 预计改动：~10 行

### Step 4：TypeScript 编译验证
- 执行 `npx tsc --noEmit`
- 确保零报错

### Step 5：运行验证
- 启动开发服务器
- 验证个人中心显示、Dropdown 展开、Drawer 滑出、状态持久化、退出登录

---

## 九、验收标准

| 序号 | 验收项 | 通过标准 |
|---|---|---|
| 1 | 个人中心显示 | Sider 底部正确显示头像、昵称、「免费」标签、速通次数 |
| 2 | Dropdown 交互 | 点击触发区展开下拉菜单，含 9 个菜单项 + 2 条分割线 |
| 3 | 消息红点 | 「消息」菜单项右侧显示 Badge 红点，数字为 `unreadMessageCount` |
| 4 | Drawer 打开 | 点击「设置」右侧滑出 Drawer，标题「账号」，宽度 420px |
| 5 | Drawer 内容 | 含用户信息卡片、账号信息（ID + 脱敏手机号）、速通用量、隐私模式 Switch、消息通知 Switch、主题 Segmented、语言 Select、退出登录按钮 |
| 6 | 状态持久化 | 修改主题/语言/隐私模式后刷新页面，设置值从 `localStorage` 正确恢复 |
| 7 | 退出登录 | 点击「退出登录」清除 `ai_mate_token` 和 `ai-mate-settings`，页面刷新 |
| 8 | 关闭 Drawer | 点击关闭按钮、点击遮罩层、按 ESC 键，Drawer 正确关闭 |
| 9 | TypeScript | `npx tsc --noEmit` 零报错 |
| 10 | 视觉一致性 | 颜色、字体、间距符合设计规范，与现有深色侧边栏风格统一 |

---

## 十、文件改动清单

| 文件 | 操作 | 改动内容 | 预计行数 |
|---|---|---|---|
| `src/components/UserProfileMenu.tsx` | 新增 | 个人中心触发按钮 + Dropdown 下拉菜单 | ~120 |
| `src/components/SettingsDrawer.tsx` | 新增 | 右侧 Drawer，含账号、权益、速通、偏好、外观、退出 | ~200 |
| `src/App.tsx` | 修改 | 引入组件、替换底部、挂载 Drawer | ~10 |

**总计**：新增 2 个文件，修改 1 个文件，预计新增代码 ~330 行。

---

## 十一、后续可扩展项

| 功能 | 说明 | 优先级 |
|---|---|---|
| 真实登录态对接 | 替换 mock `userInfo`，接入后端登录接口 | P1 |
| 全局暗色模式 | 基于 `settings.theme` 切换全局主题变量 | P2 |
| 语言国际化 | 基于 `settings.language` 接入 i18n | P2 |
| 头像上传 | 支持用户上传自定义头像 | P3 |
| 消息中心页面 | 实现 Dropdown「消息」入口的完整页面 | P3 |
| 账户管理页面 | 实现 Dropdown「管理账户」入口的完整页面 | P3 |
| 升级权益弹窗 | 实现 Dropdown「升级权益」的付费流程 | P3 |
